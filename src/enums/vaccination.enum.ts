enum STATUS {
    BOOKED = 'BOOKED',
    CANCELLED = 'CANCELLED',
    COMPLETED ='COMPLETED',
    PENDING ='PENDING'
}

enum NUMBER {
    FIRST = 'FIRST',
    SECOND = 'SECOND'
}

module.exports = {
    STATUS,
    NUMBER
}